# Local development only. This path is read by the Vite development middleware
# and is never bundled into the browser build.
COMPANION_VRM_PATH=/absolute/path/to/private-model.vrm

# Optional. The default is 5184 to avoid the active Life OS development port.
COMPANION_STAGE_PORT=5184
